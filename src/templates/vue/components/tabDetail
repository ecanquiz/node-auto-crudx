<script setup lang="ts">
  import {ref} from 'vue'
  import Form<%= tableDetailCurrent.tableNameSingularUCamelCase %> from './Form<%= tableDetailCurrent.tableNameSingularUCamelCase %>.vue'
  import Table<%= tableDetailCurrent.tableNameSingularUCamelCase %> from './Table<%= tableDetailCurrent.tableNameSingularUCamelCase %>.vue'
  import useTab<%= tableDetailCurrent.tableNameSingularUCamelCase %> from '../../composables/<%= tableMasterSingularUCamelCase; %>/useTab<%= tableDetailCurrent.tableNameSingularUCamelCase %>'
  const componentKey = ref(0);
  
  const props = defineProps<{ id?: string }>()
  
  const {
    panelOpened,
    closeButtonOpened,
    closeClassOpened,  
    <%= tableDetailCurrent.tableName %>,
    <%= tableDetailCurrent.tableNameSingular %>,
  
    edit<%= tableDetailCurrent.tableNameSingularUCamelCase %>,
    get<%= tableDetailCurrent.tableNameUCamelCase %>,
    remove<%= tableDetailCurrent.tableNameSingularUCamelCase %>,  
    submit<%= tableDetailCurrent.tableNameSingularUCamelCase %>
  } = useTab<%= tableDetailCurrent.tableNameSingularUCamelCase %>(props.id)
  </script>
  
  <template>
    <div class="demo-tab">
      <div class="form-group row">
        <div class="col-sm-12">
          <div class="grid justify-items-stretch">
            <AppBtn
              class="btn p-8 justify-self-center"
              type="text"                 
              data-testid="click-btn"
              :class="closeClassOpened"
              :text="`${closeButtonOpened} panel to add <%= tableDetailCurrent.tableNameSingularUCamelCase %>`"
              @click="panelOpened=!panelOpened"
            />      
            <Form<%= tableDetailCurrent.tableNameSingularUCamelCase %>
              v-if="panelOpened"
              class="bg-base-200 py-4 mt-2 rounded"
              :<%= tableDetailCurrent.tableNameSingular %>="<%= tableDetailCurrent.tableNameSingular %>"
              @submit<%= tableDetailCurrent.tableNameSingularUCamelCase %>="submit<%= tableDetailCurrent.tableNameSingularUCamelCase %>"
            />
            <Table<%= tableDetailCurrent.tableNameSingularUCamelCase %>
              :key="componentKey"
              v-if="<%= tableDetailCurrent.tableName %>"
              :<%= tableDetailCurrent.tableName %>="<%= tableDetailCurrent.tableName %>"
              @edit<%= tableDetailCurrent.tableNameSingularUCamelCase %>="edit<%= tableDetailCurrent.tableNameSingularUCamelCase %>"
              @get<%= tableDetailCurrent.tableNameUCamelCase %>="get<%= tableDetailCurrent.tableNameUCamelCase %>"
              @remove<%= tableDetailCurrent.tableNameSingularUCamelCase %>="remove<%= tableDetailCurrent.tableNameSingularUCamelCase %>"            
            />
          </div>
        </div>
      </div>
    </div>
  </template>
  
