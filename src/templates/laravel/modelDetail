<?php

namespace Modules\<%= pathModule; %>\Entities;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Illuminate\Database\Eloquent\Model;

class <%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)); %> extends Model
{
    use HasFactory, SoftDeletes;

    protected $connection = 'pgsql_<%= pathModule.toLowerCase(); %>';

    protected $fillable = [<% tableDetailCurrent.tableStructureOfDetails[0].forEach(function(field, index) {
      if  (fn.v.excludeFields(field.column_name)) {%>
        '<%- field.column_name; %>'<%= fn.addCommaToArr(tableStructure, index, 0) -%>
    <%}});%> 
    ];

    protected $hidden = ['created_at', 'updated_at', 'deleted_at'];

    protected $casts = [ /* 'field_name' => 'field_type' */ ];
        
    public function <%= fn.singular(tableMaster); %>()
    {
        return $this->belongsTo(\Modules\<%= pathModule; %>\Entities\<%= fn.uCamelCase(fn.singular(tableMaster)); %>::class);
    }  

    protected static function newFactory()
    {
        return \Modules\<%= pathModule; %>\Database\Factories\<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)); %>Factory::new();
    }
}
