<?php

namespace Modules\<%= pathModule; %>\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\Http\JsonResponse;
use Illuminate\Routing\Controller;
use Illuminate\Database\Eloquent\Collection;
use Modules\<%= pathModule; %>\Entities\<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>;
use Modules\<%= pathModule; %>\Http\Requests\<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>\{
    Store<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Request,
    Update<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Request    
};
use Modules\<%= pathModule; %>\Http\Services\<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>\{
    Store<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Service,
    Update<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Service
}; 

class <%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Controller extends Controller
{
    /**
     * Display a listing of the resource by parent.
     */
    public function getAllBy<%= tableMasterSingularUCamelCase ;%>(Request $request)//: Collection
    {
        return <%= tableDetailCurrent.tableNameSingularUCamelCase ;%>::where('<%= tableDetailCurrent.idForeignKey %>', $request-><%= fn.camelCase(tableDetailCurrent.idForeignKey) %>)->get();
    }

    /**
     * Store a newly created resource in storage.
     */    
    public function store(Store<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Request $request): JsonResponse
    {    
        return Store<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Service::execute($request);
    }
    
    /**
     * Update the specified resource in storage.
     */
    public function update(Update<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Request $request, <%= tableDetailCurrent.tableNameSingularUCamelCase ;%> $<%= tableDetailCurrent.tableNameSingular ;%>): JsonResponse
    {
        return Update<%= tableDetailCurrent.tableNameSingularUCamelCase ;%>Service::execute($request, $<%= tableDetailCurrent.tableNameSingular ;%>);
    }

    /**
     * Remove the specified resource from storage.
     */
    public function destroy(Request $request): JsonResponse
    {
        <%= tableDetailCurrent.tableNameSingularUCamelCase ;%>::destroy($request->id);

        return response()->json(204);            
    }
}

