import Http from "@/utils/Http";
import type { <%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %> } from "../types/<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %>";

export const get<%= fn.uCamelCase(tableDetailCurrent.tableName) %> = (<%= fn.singular(tableMaster); %>Id: string) => {  
  return Http.get(`/api/<%= tableDetailCurrent.tableName %>/${<%= fn.singular(tableMaster); %>Id}`);
}

export const insert<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %> = (payload: <%= fn.uCamelCase(tableDetailCurrent.tableName) %>) => {   
  return Http.post(`/api/<%= tableDetailCurrent.tableName %>`, payload);
}

export const update<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %> = (payload: <%= fn.uCamelCase(tableDetailCurrent.tableName) %>, <%= fn.singular(tableDetailCurrent.tableName) %>Id: string) => {   
  return Http.put(`/api/<%= tableDetailCurrent.tableName %>/${<%= fn.singular(tableDetailCurrent.tableName) %>Id}`, payload);
}

export const delete<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %> = (<%= fn.singular(tableDetailCurrent.tableName) %>Id: string) => { 
  return Http.delete(`/api/<%= tableDetailCurrent.tableName %>/${<%= fn.singular(tableDetailCurrent.tableName) %>Id}`);
}

export default {
  get<%= fn.uCamelCase(tableDetailCurrent.tableName) %>,
  insert<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %>,
  update<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %>,
  delete<%= fn.uCamelCase(fn.singular(tableDetailCurrent.tableName)) %>
}
